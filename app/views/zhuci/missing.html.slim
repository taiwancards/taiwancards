- content_for(:title, params[:text])

.py-10.space-y-4.text-center
  .text-5xl.font-semibold lang="zh-TW" = params[:text]
  p.text-sm.text-muted-foreground = t("zhuci.not_found")
  = link_to t("zhuci.back"), zhuci_path, class: "inline-flex items-center gap-1 text-sm text-primary hover:underline"
